[pytest]
addopts = -v --tb=short --strict -k 'not postponed'
markers = 
    apply_rules: Run @pytest.mark.apply_rules functions
    line_matches: Run @pytest.mark.line_matches functions with `pytest -v -m line_matches`
    rule: Run @pytest.mark.rule functions
    focus: Whatever test functions are currently focus of attention
